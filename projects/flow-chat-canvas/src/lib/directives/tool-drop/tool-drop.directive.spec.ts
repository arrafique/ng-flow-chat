import { CanvasToolDropDirective } from './tool-drop.directive';

describe('ToolDropDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasToolDropDirective();
    expect(directive).toBeTruthy();
  });
});
