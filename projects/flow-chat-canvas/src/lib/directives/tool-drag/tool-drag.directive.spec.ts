import { ToolDragDirective } from './tool-drag.directive';

describe('ToolDragDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolDragDirective();
    expect(directive).toBeTruthy();
  });
});
