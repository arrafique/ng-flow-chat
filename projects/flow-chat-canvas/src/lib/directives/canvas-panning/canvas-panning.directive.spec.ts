import { CanvasPanningDirective } from './canvas-panning.directive';

describe('CanvasPanningDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasPanningDirective();
    expect(directive).toBeTruthy();
  });
});
